# UX Pack Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tema Claro/Oscuro, Command Palette (Ctrl+K), estados vacíos con CTA y responsive móvil — solo frontend.

**Architecture:** El tema es un segundo bloque de variables CSS (`[data-theme="light"]`) + preferencia en localStorage con anti-flash inline. El palette es un fichero nuevo `frontend/js/palette.js` con índice declarativo (secciones/acciones/recursos) verificado por el test de handlers. Estados vacíos = helper global `emptyState()` en core.js + sweep de módulos. Responsive = un bloque `@media (max-width:768px)` + sidebar off-canvas.

**Tech Stack:** Vanilla JS (sin bundler, scope global entre `<script>`), CSS variables, `node:test` para el test de red de seguridad.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-18-ux-pack-design.md`. Rama: `feat/ux-pack`.
- UI y comentarios en **español**. Sin dependencias nuevas. Cero cambios de backend salvo `backend/test/frontend-handlers.test.js`.
- Toda función usada en `onclick=` debe ser global y existir exactamente una vez (invariante del test de handlers).
- Acento ámbar `#E8A020` se mantiene en ambos temas. El terminal permanece oscuro en ambos temas.
- localStorage siempre con try/catch (modo privado estricto no debe romper nada).
- Tras cambiar JS/CSS, bumpear los `?v=` de `index.html` a `20260718`.

---

### Task 1: Rama + variables de tema y modo claro (CSS)

**Files:**
- Modify: `frontend/css/styles.css` (bloque `:root`, y ocurrencias hardcodeadas)
- Modify: `frontend/index.html` (anti-flash + `?v=` del CSS)

**Interfaces:**
- Produces: atributo `data-theme="light"` en `<html>` activa el tema claro; variables nuevas `--on-accent`, `--shadow-lg`.

- [ ] **Step 1: Crear la rama**

```bash
git checkout -b feat/ux-pack
```

- [ ] **Step 2: Extraer colores hardcodeados a variables**

En `frontend/css/styles.css`, añadir al final del bloque `:root` (tras `--transition:0.15s ease;`):

```css
  --on-accent:#0C0D0F;
  --shadow-lg:0 20px 60px rgba(0,0,0,0.5);
```

Reemplazar los usos hardcodeados (el color de texto sobre el acento y las sombras grandes):
- `.login-btn` → `color:var(--on-accent)`
- `.btn-primary` → `color:var(--on-accent)`
- `.user-avatar` → `color:var(--on-accent)`
- `.nav-badge` → `color:var(--on-accent)`
- `.login-box` → `box-shadow:var(--shadow-lg)`
- `.modal` → `box-shadow:var(--shadow-lg)`

El fondo del terminal (`#0a0a0a` en `#terminal-container` y `#xterm-mount`) NO se toca: queda oscuro en ambos temas.

- [ ] **Step 3: Añadir el bloque del tema claro**

Inmediatamente después del bloque `:root{...}` en `styles.css`:

```css
/* ── Tema claro (activado con data-theme="light" en <html>) ── */
[data-theme="light"]{
  --bg-app:#F4F5F7;
  --bg-sidebar:#FFFFFF;
  --bg-card:#FFFFFF;
  --bg-card2:#F0F1F4;
  --bg-hover:#EAECEF;
  --border:#E2E4E9;
  --border2:#D5D8DF;
  --accent:#E8A020;
  --accent2:#B57407;
  --accent-glow:rgba(232,160,32,0.10);
  --text-primary:#1A1C22;
  --text-secondary:#5A5F73;
  --text-muted:#8A8FA3;
  --green:#1F9D4D;
  --red:#D93025;
  --yellow:#9A7B00;
  --blue:#1A6FD4;
  --cyan:#0E7C9C;
  --shadow-lg:0 20px 60px rgba(0,0,0,0.15);
}
```

- [ ] **Step 4: Anti-flash en `index.html`**

En el `<head>`, ANTES del `<link rel="stylesheet" href="css/styles.css...">`:

```html
<script>
// Anti-flash: aplica el tema guardado antes del primer pintado.
(function(){try{
  var p=localStorage.getItem('txpl_theme')||'system';
  var light=p==='light'||(p==='system'&&window.matchMedia&&matchMedia('(prefers-color-scheme: light)').matches);
  if(light)document.documentElement.setAttribute('data-theme','light');
}catch(e){}})();
</script>
```

Bumpear el CSS: `css/styles.css?v=20260718`.

- [ ] **Step 5: Verificar a mano y commit**

Arrancar `npm run dev`, en devtools ejecutar `document.documentElement.setAttribute('data-theme','light')` y comprobar que login y dashboard se ven claros y legibles.

```bash
git add frontend/css/styles.css frontend/index.html
git commit -m "feat(ui): tema claro por variables CSS + anti-flash"
```

---

### Task 2: Controles de tema (topbar + Ajustes + core.js)

**Files:**
- Modify: `frontend/js/core.js` (funciones de tema, cierre en `bootApp`)
- Modify: `frontend/index.html` (botón topbar, `?v=` de los scripts)
- Modify: `frontend/views/pages/settings.html` (tarjeta Apariencia)

**Interfaces:**
- Produces: globals `themePref()`, `applyTheme(pref)`, `setThemePref(pref)`, `toggleTheme()`.

- [ ] **Step 1: Test primero — añadir los onclick sin implementar**

En `frontend/index.html`, dentro del div de la derecha de la topbar, antes del botón "Salir":

```html
        <button class="btn btn-sm" onclick="toggleTheme()" title="Cambiar tema" aria-label="Cambiar tema">
          <i id="theme-toggle-icon" class="ti ti-sun"></i>
        </button>
```

En `frontend/views/pages/settings.html`, dentro del `overview-grid` inicial, añadir una tarjeta al final del grid:

```html
  <div class="card">
    <div class="card-header">
      <div class="card-title"><i class="ti ti-palette"></i> Apariencia</div>
    </div>
    <div class="form-group">
      <label>Tema del panel</label>
      <select id="set-theme" onchange="setThemePref(this.value)">
        <option value="system">Según el sistema</option>
        <option value="dark">Oscuro</option>
        <option value="light">Claro</option>
      </select>
    </div>
    <p class="text-muted" style="font-size:12px">"Según el sistema" sigue la preferencia de tu dispositivo.</p>
  </div>
```

- [ ] **Step 2: Verificar que el test falla**

Run: `node --test backend/test/frontend-handlers.test.js`
Expected: FAIL — `Handlers sin definición: toggleTheme, setThemePref`

- [ ] **Step 3: Implementar las funciones de tema en `core.js`**

Añadir tras la sección de helpers (después de `closeModal`):

```js
// ── Tema (claro/oscuro/sistema) ──────────────────────────────
// La preferencia vive en localStorage ('txpl_theme'); el anti-flash del
// index.html la aplica antes del primer pintado. Aquí solo se gestiona
// el cambio en caliente y la sincronización de los controles.
function themePref() {
  try { return localStorage.getItem('txpl_theme') || 'system'; } catch (_) { return 'system'; }
}
function applyTheme(pref) {
  const light = pref === 'light' ||
    (pref === 'system' && window.matchMedia && matchMedia('(prefers-color-scheme: light)').matches);
  document.documentElement.setAttribute('data-theme', light ? 'light' : 'dark');
  const ic = document.getElementById('theme-toggle-icon');
  if (ic) ic.className = 'ti ti-' + (light ? 'moon' : 'sun');
}
function setThemePref(pref) {
  try { localStorage.setItem('txpl_theme', pref); } catch (_) {}
  applyTheme(pref);
  const sel = document.getElementById('set-theme');
  if (sel) sel.value = pref;
}
function toggleTheme() {
  const light = document.documentElement.getAttribute('data-theme') === 'light';
  setThemePref(light ? 'dark' : 'light');
}
// Si la preferencia es "system", seguir los cambios del SO en vivo.
if (window.matchMedia) {
  const mq = matchMedia('(prefers-color-scheme: light)');
  if (mq.addEventListener) mq.addEventListener('change', () => { if (themePref() === 'system') applyTheme('system'); });
}
```

En `bootApp()`, tras `bindModalOverlayEvents();` añadir:

```js
  applyTheme(themePref());
  const themeSel = document.getElementById('set-theme');
  if (themeSel) themeSel.value = themePref();
```

- [ ] **Step 4: Verificar que el test pasa**

Run: `npm test`
Expected: PASS (todos)

- [ ] **Step 5: Bump `?v=` y commit**

Bumpear todos los `js/*.js?v=` de `index.html` a `20260718`.

```bash
git add frontend/js/core.js frontend/index.html frontend/views/pages/settings.html
git commit -m "feat(ui): selector de tema en Ajustes + toggle sol/luna en topbar"
```

---

### Task 3: Command Palette (Ctrl+K)

**Files:**
- Create: `frontend/js/palette.js`
- Modify: `frontend/index.html` (script nuevo + markup del palette)
- Modify: `frontend/css/styles.css` (estilos)
- Test: `backend/test/frontend-handlers.test.js` (test nuevo)

**Interfaces:**
- Consumes: `navigate(page)`, `openModal(id)`, `req(method, path)` de `core.js`; funciones globales de módulos (`resetDeployModal`, `backupNow`).
- Produces: `PALETTE_SECTIONS`, `PALETTE_ACTIONS`, `openPalette()`, `closePalette()` globales.

- [ ] **Step 1: Escribir el test que falla**

Añadir a `backend/test/frontend-handlers.test.js`:

```js
test('palette: acciones referencian funciones, páginas y modales existentes', () => {
  const paletteSrc = fs.readFileSync(path.join(FRONTEND, 'js', 'palette.js'), 'utf8');
  const m = paletteSrc.match(/const PALETTE_ACTIONS = (\[[\s\S]*?\n\]);/);
  assert.ok(m, 'PALETTE_ACTIONS no encontrado en palette.js');
  const actions = new Function(`return ${m[1]}`)(); // array literal, sin referencias externas

  const jsFiles = walk(path.join(FRONTEND, 'js'), '.js');
  const defined = new Set();
  for (const f of jsFiles) for (const n of definedTopLevel(fs.readFileSync(f, 'utf8'))) defined.add(n);

  const htmlAll = [path.join(FRONTEND, 'index.html'), ...walk(path.join(FRONTEND, 'views'), '.html')]
    .map((f) => fs.readFileSync(f, 'utf8')).join('\n');

  for (const a of actions) {
    assert.ok(a.label && a.page, `Acción sin label/page: ${JSON.stringify(a)}`);
    assert.ok(htmlAll.includes(`id="page-${a.page}"`), `Página inexistente: ${a.page}`);
    if (a.fn) assert.ok(defined.has(a.fn), `Función inexistente en acción "${a.label}": ${a.fn}`);
    if (a.modal) assert.ok(htmlAll.includes(`id="${a.modal}"`), `Modal inexistente en acción "${a.label}": ${a.modal}`);
  }
});
```

- [ ] **Step 2: Verificar que falla**

Run: `node --test backend/test/frontend-handlers.test.js`
Expected: FAIL — `ENOENT ... palette.js`

- [ ] **Step 3: Crear `frontend/js/palette.js`**

```js
// TecXPaneL — Command Palette (Ctrl+K / Cmd+K)
// Búsqueda global: secciones, acciones y recursos (sitios, apps, BDs,
// contenedores). Índice estático + recursos con caché de 60 s.
// Sin librerías; requiere core.js (navigate, openModal, req, esc).

const PALETTE_SECTIONS = [
  { page: 'dashboard', label: 'Dashboard', alias: 'inicio panel', icon: 'layout-dashboard' },
  { page: 'websites',  label: 'Sitios Web', alias: 'webs dominios nginx', icon: 'world' },
  { page: 'apps',      label: 'Aplicaciones', alias: 'deploy pm2 node', icon: 'brand-nodejs' },
  { page: 'databases', label: 'Bases de Datos', alias: 'mysql postgres bbdd', icon: 'database' },
  { page: 'docker',    label: 'Docker', alias: 'contenedores containers', icon: 'brand-docker' },
  { page: 'n8n',       label: 'Workflows', alias: 'n8n automatizaciones', icon: 'sitemap' },
  { page: 'catalog',   label: 'Catálogo de aplicaciones', alias: 'wordpress ghost one-click', icon: 'apps' },
  { page: 'backups',   label: 'Copias de seguridad', alias: 'backups restaurar', icon: 'archive' },
  { page: 'cron',      label: 'Tareas programadas', alias: 'cron crontab', icon: 'clock' },
  { page: 'mail',      label: 'Correo', alias: 'email buzones mail', icon: 'mail' },
  { page: 'dns',       label: 'DNS', alias: 'zonas registros powerdns', icon: 'world-search' },
  { page: 'files',     label: 'Gestor de Archivos', alias: 'ficheros archivos explorador', icon: 'folder' },
  { page: 'firewall',  label: 'Firewall UFW', alias: 'cortafuegos puertos reglas', icon: 'shield' },
  { page: 'ssl',       label: 'Certificados SSL', alias: 'https certbot letsencrypt', icon: 'certificate' },
  { page: 'logs',      label: 'Logs del sistema', alias: 'registros errores', icon: 'file-text' },
  { page: 'terminal',  label: 'Terminal SSH', alias: 'consola shell', icon: 'terminal-2' },
  { page: 'plugins',   label: 'Plugins', alias: 'paquetes instalar', icon: 'puzzle' },
  { page: 'help',      label: 'Manual de uso', alias: 'ayuda documentacion', icon: 'lifebuoy' },
  { page: 'settings',  label: 'Ajustes', alias: 'configuracion tema contraseña', icon: 'settings' },
];

const PALETTE_ACTIONS = [
  { label: 'Crear sitio web', page: 'websites', modal: 'modal-new-site', icon: 'world-plus' },
  { label: 'Desplegar aplicación', page: 'apps', fn: 'resetDeployModal', modal: 'modal-new-app', icon: 'rocket' },
  { label: 'Nueva base de datos', page: 'databases', modal: 'modal-new-db', icon: 'database-plus' },
  { label: 'Nuevo contenedor Docker', page: 'docker', modal: 'modal-new-container', icon: 'cube-plus' },
  { label: 'Nueva regla de firewall', page: 'firewall', modal: 'modal-new-rule', icon: 'shield-plus' },
  { label: 'Nueva carpeta', page: 'files', modal: 'modal-new-folder', icon: 'folder-plus' },
  { label: 'Nuevo archivo', page: 'files', modal: 'modal-new-file', icon: 'file-plus' },
  { label: 'Backup completo ahora', page: 'backups', fn: 'backupNow', args: ['full'], icon: 'player-record' },
];

// Recursos dinámicos: se cargan al abrir con caché de 60 s.
let paletteCache = { at: 0, items: [] };

function paletteNorm(s) {
  return String(s || '').normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase();
}

async function paletteLoadResources() {
  if (Date.now() - paletteCache.at < 60000) return paletteCache.items;
  const sources = [
    { path: '/websites', page: 'websites', icon: 'world', pick: (d) => (Array.isArray(d) ? d : []).map((w) => w.domain) },
    { path: '/apps', page: 'apps', icon: 'brand-nodejs', pick: (d) => (Array.isArray(d) ? d : []).map((a) => a.name) },
    { path: '/databases', page: 'databases', icon: 'database', pick: (d) => (Array.isArray(d) ? d : []).map((db) => db.name) },
    { path: '/docker/containers', page: 'docker', icon: 'brand-docker', pick: (d) => (d?.containers || (Array.isArray(d) ? d : [])).map((c) => c.name || (c.Names && c.Names[0]) || '').filter(Boolean) },
  ];
  const results = await Promise.allSettled(sources.map((s) => req('GET', s.path)));
  const items = [];
  results.forEach((r, i) => {
    if (r.status !== 'fulfilled' || !r.value) return; // API caída: fuente omitida
    try {
      for (const name of sources[i].pick(r.value)) {
        items.push({ label: name, page: sources[i].page, icon: sources[i].icon, kind: 'Recursos' });
      }
    } catch (_) { /* forma inesperada: fuente omitida */ }
  });
  paletteCache = { at: Date.now(), items };
  return items;
}

let paletteSel = 0;
let paletteItems = [];

function paletteIndex(resources) {
  return [
    ...PALETTE_SECTIONS.map((s) => ({ ...s, kind: 'Secciones' })),
    ...PALETTE_ACTIONS.map((a) => ({ ...a, kind: 'Acciones' })),
    ...resources,
  ];
}

function paletteFilter(all, q) {
  const nq = paletteNorm(q);
  const hits = !nq ? all : all.filter((it) => paletteNorm(it.label + ' ' + (it.alias || '')).includes(nq));
  return hits.slice(0, 12);
}

function paletteRender() {
  const list = document.getElementById('palette-list');
  if (!paletteItems.length) {
    list.innerHTML = '<div class="palette-empty">Sin resultados</div>';
    return;
  }
  let html = '', lastKind = '';
  paletteItems.forEach((it, i) => {
    if (it.kind !== lastKind) { html += `<div class="palette-group">${esc(it.kind)}</div>`; lastKind = it.kind; }
    html += `<div class="palette-item${i === paletteSel ? ' sel' : ''}" onclick="paletteExec(${i})">` +
      `<i class="ti ti-${esc(it.icon || 'chevron-right')}"></i> ${esc(it.label)}</div>`;
  });
  list.innerHTML = html;
  const sel = list.querySelector('.palette-item.sel');
  if (sel) sel.scrollIntoView({ block: 'nearest' });
}

async function paletteUpdate() {
  const q = document.getElementById('palette-input').value;
  const resources = await paletteLoadResources();
  paletteItems = paletteFilter(paletteIndex(resources), q);
  if (paletteSel >= paletteItems.length) paletteSel = 0;
  paletteRender();
}

function paletteExec(i) {
  const it = paletteItems[i];
  if (!it) return;
  closePalette();
  navigate(it.page);
  document.querySelectorAll('.nav-item').forEach((n) => n.classList.toggle('active', n.dataset.page === it.page));
  if (it.fn && typeof window[it.fn] === 'function') window[it.fn](...(it.args || []));
  if (it.modal) openModal(it.modal);
}

function openPalette() {
  paletteSel = 0;
  document.getElementById('palette-overlay').classList.add('open');
  const input = document.getElementById('palette-input');
  input.value = '';
  input.focus();
  paletteUpdate();
}

function closePalette() {
  document.getElementById('palette-overlay').classList.remove('open');
}

document.addEventListener('keydown', (e) => {
  if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === 'k') {
    e.preventDefault();
    const open = document.getElementById('palette-overlay')?.classList.contains('open');
    if (open) closePalette(); else if (TOKEN) openPalette();
    return;
  }
  const overlay = document.getElementById('palette-overlay');
  if (!overlay || !overlay.classList.contains('open')) return;
  if (e.key === 'Escape') { closePalette(); }
  else if (e.key === 'ArrowDown') { e.preventDefault(); paletteSel = Math.min(paletteSel + 1, paletteItems.length - 1); paletteRender(); }
  else if (e.key === 'ArrowUp') { e.preventDefault(); paletteSel = Math.max(paletteSel - 1, 0); paletteRender(); }
  else if (e.key === 'Enter') { e.preventDefault(); paletteExec(paletteSel); }
});
```

- [ ] **Step 4: Markup y estilos**

En `frontend/index.html`, antes de `<div class="toast" id="toast"></div>`:

```html
<!-- Command Palette (Ctrl+K) -->
<div id="palette-overlay" onclick="if(event.target===this)closePalette()">
  <div class="palette-box">
    <input id="palette-input" type="text" placeholder="Buscar sección, acción o recurso…  (Ctrl+K)" oninput="paletteUpdate()" autocomplete="off">
    <div id="palette-list"></div>
  </div>
</div>
```

Y el script tras `core.js`:

```html
<script src="js/palette.js?v=20260718"></script>
```

Nota: el `oninput` no está en la lista de eventos del test de handlers (`onclick|onchange|oninput` sí incluye `input`) — no requiere cambios extra.

En `frontend/css/styles.css`, tras la sección de modales:

```css
/* ── Command Palette ── */
#palette-overlay{
  position:fixed;inset:0;background:rgba(0,0,0,0.6);z-index:700;
  display:none;align-items:flex-start;justify-content:center;padding-top:12vh;
}
#palette-overlay.open{display:flex}
.palette-box{
  background:var(--bg-card);border:1px solid var(--border2);border-radius:var(--radius-lg);
  width:100%;max-width:560px;box-shadow:var(--shadow-lg);overflow:hidden;
}
#palette-input{
  width:100%;background:transparent;border:none;border-bottom:1px solid var(--border);
  color:var(--text-primary);padding:14px 16px;font-family:var(--font);font-size:15px;outline:none;
}
#palette-list{max-height:50vh;overflow-y:auto;padding:6px 0}
.palette-group{padding:8px 16px 4px;font-size:10px;font-weight:600;letter-spacing:1.5px;text-transform:uppercase;color:var(--text-muted)}
.palette-item{
  display:flex;align-items:center;gap:10px;padding:9px 16px;font-size:13.5px;
  color:var(--text-secondary);cursor:pointer;
}
.palette-item i{font-size:17px;color:var(--text-muted)}
.palette-item:hover,.palette-item.sel{background:var(--bg-hover);color:var(--text-primary)}
.palette-item.sel i{color:var(--accent)}
.palette-empty{padding:1.5rem;text-align:center;color:var(--text-muted);font-size:13px}
```

- [ ] **Step 5: Verificar tests y a mano**

Run: `npm test`
Expected: PASS. A mano: Ctrl+K abre, escribir "correo" muestra la sección Correo, "crear" muestra acciones, Enter navega/abre modal, Esc cierra.

- [ ] **Step 6: Commit**

```bash
git add frontend/js/palette.js frontend/index.html frontend/css/styles.css backend/test/frontend-handlers.test.js
git commit -m "feat(ui): command palette Ctrl+K con secciones, acciones y recursos"
```

---

### Task 4: Estados vacíos con CTA

**Files:**
- Modify: `frontend/js/core.js` (helper `emptyState`)
- Modify: `frontend/css/styles.css` (variante con botón)
- Modify: `frontend/js/websites.js:9`, `frontend/js/apps.js:8`, `frontend/js/databases.js:14`, `frontend/js/firewall.js:13`, `frontend/js/docker.js:19`, `frontend/js/ssl.js:40` (y resto de módulos con listados)

**Interfaces:**
- Consumes: `openModal(id)` y funciones de creación existentes.
- Produces: global `emptyState(icon, message, ctaLabel, ctaOnclick)` → string HTML (contenido interno, SIN el wrapper `.empty-state`).

- [ ] **Step 1: Helper en `core.js`**

Tras `closeModal`:

```js
// emptyState: contenido de un estado vacío (icono + mensaje + CTA opcional).
// Devuelve el HTML interno; el llamador lo envuelve en su celda/div con
// class="empty-state". ctaOnclick es código inline (patrón onclick del panel).
function emptyState(icon, message, ctaLabel, ctaOnclick) {
  const cta = ctaLabel
    ? `<br><button class="btn btn-primary btn-sm mt-2" onclick="${esc(ctaOnclick)}"><i class="ti ti-plus"></i> ${esc(ctaLabel)}</button>`
    : '';
  return `<i class="ti ti-${esc(icon)}"></i><br>${esc(message)}${cta}`;
}
```

En `styles.css`, junto a `.empty-state`:

```css
.empty-state .btn{margin-top:1rem}
```

- [ ] **Step 2: Sweep de módulos**

Reemplazos exactos (mantener el `<tr><td colspan=...>` de cada uno):

`frontend/js/websites.js:9`:
```js
  if (!data.length) { tb.innerHTML = '<tr><td colspan="6" class="empty-state">' + emptyState('world-off', 'Sin sitios web aún', 'Crear sitio web', "openModal('modal-new-site')") + '</td></tr>'; return; }
```

`frontend/js/apps.js:8`:
```js
  if (!data.length) { tb.innerHTML = '<tr><td colspan="6" class="empty-state">' + emptyState('brand-nodejs', 'Sin aplicaciones aún', 'Desplegar aplicación', "resetDeployModal();openModal('modal-new-app')") + '</td></tr>'; return; }
```

`frontend/js/databases.js:14`:
```js
  if (!data.length) { tb.innerHTML = '<tr><td colspan="8" class="empty-state">' + emptyState('database-off', 'Sin bases de datos', 'Nueva base de datos', "openModal('modal-new-db')") + '</td></tr>'; return; }
```

`frontend/js/firewall.js:13`:
```js
  if (!rules.length) { tb.innerHTML = '<tr><td colspan="5" class="empty-state">' + emptyState('shield-off', 'Sin reglas de firewall', 'Nueva regla', "openModal('modal-new-rule')") + '</td></tr>'; return; }
```

`frontend/js/docker.js:19` (solo el caso "sin contenedores"; los casos de error/no instalado se quedan como están):
```js
    tb.innerHTML = '<tr><td colspan="6" class="empty-state">' + emptyState('brand-docker', 'No hay contenedores creados en el sistema', 'Nuevo contenedor', "openModal('modal-new-container')") + '</td></tr>';
```

`frontend/js/ssl.js:40` (sin CTA — emitir requiere flujo propio):
```js
  ... : '<tr><td colspan="5" class="empty-state">' + emptyState('certificate-off', 'No hay certificados emitidos') + '</td></tr>';
```

- [ ] **Step 3: Módulos con listados sin estado vacío (dns, cron, backups, mail, catalog)**

Localizar los renders de listas con `grep -n "innerHTML" frontend/js/dns.js frontend/js/cron.js frontend/js/backups.js frontend/js/mail.js frontend/js/catalog.js`. En cada render de lista donde una colección vacía deje la tabla/lista en blanco, añadir el patrón (mensaje sin CTA, porque estos módulos crean con formularios inline, no modales):

```js
  if (!items.length) { el.innerHTML = '<tr><td colspan="N" class="empty-state">' + emptyState('ICONO', 'MENSAJE') + '</td></tr>'; return; }
```

Con estos valores por módulo (ajustar `colspan` al número real de columnas de cada tabla):
- dns (zonas): icono `world-search`, "Sin zonas DNS aún — crea la primera con el formulario de arriba"
- cron (tareas): icono `clock-off`, "Sin tareas programadas — usa el formulario para crear la primera"
- backups (listado): icono `archive-off`, "Sin copias de seguridad aún — pulsa «Backup ahora»"
- mail (buzones): icono `mail-off`, "Sin buzones aún"
- mail (alias): icono `at-off`, "Sin alias"
- catalog (instaladas): icono `apps-off`, "Sin aplicaciones instaladas desde el catálogo"

Si un render ya muestra su propio mensaje de vacío, migrarlo a `emptyState()` conservando su texto.

- [ ] **Step 4: Verificar tests y a mano**

Run: `npm test`
Expected: PASS. A mano: con la BD de dev vacía, cada sección muestra icono + mensaje (+ botón donde aplica) y el botón abre el modal correcto.

- [ ] **Step 5: Commit**

```bash
git add frontend/js/ frontend/css/styles.css
git commit -m "feat(ui): estados vacíos con CTA en todos los listados"
```

---

### Task 5: Responsive móvil

**Files:**
- Modify: `frontend/css/styles.css` (bloque media query)
- Modify: `frontend/index.html` (hamburguesa + overlay de sidebar)
- Modify: `frontend/js/core.js` (`toggleSidebar`, cierre en `navigate`)

**Interfaces:**
- Produces: global `toggleSidebar()`; clase `sidebar-open` en `<body>`.

- [ ] **Step 1: Test primero — markup con onclick**

En `frontend/index.html`, como PRIMER elemento dentro de `<div class="topbar">`:

```html
      <button class="btn btn-sm sidebar-burger" onclick="toggleSidebar()" aria-label="Abrir menú">
        <i class="ti ti-menu-2"></i>
      </button>
```

Y justo después de `</aside>` (fuera de la sidebar):

```html
  <div id="sidebar-overlay" onclick="toggleSidebar()"></div>
```

Run: `node --test backend/test/frontend-handlers.test.js`
Expected: FAIL — `Handlers sin definición: toggleSidebar`

- [ ] **Step 2: Implementar en `core.js`**

Tras las funciones de tema:

```js
// ── Sidebar móvil (off-canvas) ───────────────────────────────
function toggleSidebar() {
  document.body.classList.toggle('sidebar-open');
}
```

Y en `navigate()`, tras `currentPage = page;`:

```js
  document.body.classList.remove('sidebar-open'); // cierra la sidebar móvil al navegar
```

Run: `npm test` → Expected: PASS

- [ ] **Step 3: CSS responsive**

Al final de `styles.css`:

```css
/* ── Responsive móvil (≤768px) ── */
.sidebar-burger{display:none}
#sidebar-overlay{display:none}

@media (max-width:768px){
  .sidebar-burger{display:inline-flex}
  .sidebar{
    position:fixed;left:0;top:0;bottom:0;z-index:650;
    transform:translateX(-100%);transition:transform 0.2s ease;
    box-shadow:var(--shadow-lg);
  }
  body.sidebar-open .sidebar{transform:none}
  #sidebar-overlay{
    display:block;position:fixed;inset:0;background:rgba(0,0,0,0.5);
    z-index:640;opacity:0;pointer-events:none;transition:opacity 0.2s ease;
  }
  body.sidebar-open #sidebar-overlay{opacity:1;pointer-events:all}

  .topbar{padding:0 0.75rem;gap:0.5rem}
  .topbar-subtitle,#uptime-display{display:none}
  .content{padding:0.75rem}
  .form-row{grid-template-columns:1fr}
  .overview-grid{grid-template-columns:1fr}
  .modal{max-width:calc(100vw - 1rem);max-height:calc(100dvh - 1rem)}
  .modal-overlay{padding:0.5rem}
  .palette-box{max-width:calc(100vw - 1rem)}
  #palette-overlay{padding-top:4vh}
  .stat-value{font-size:1.6rem}
}
```

- [ ] **Step 4: Verificar a mano**

Devtools en 375×812: hamburguesa abre/cierra sidebar, navegar la cierra, overlay la cierra, modales caben, tablas scrollean horizontal, terminal usable, dashboard legible.

- [ ] **Step 5: Commit**

```bash
git add frontend/css/styles.css frontend/index.html frontend/js/core.js
git commit -m "feat(ui): responsive móvil — sidebar off-canvas, modales y grids adaptados"
```

---

### Task 6: Docs + verificación final

**Files:**
- Modify: `README.md` (sección de características/frontend)
- Modify: `CLAUDE.md` (descripción del frontend: añadir `palette.js`, tema claro/oscuro)

**Interfaces:** N/A (documentación).

- [ ] **Step 1: Actualizar CLAUDE.md**

En la sección **Frontend**, añadir `palette.js` a la lista de ficheros por dominio y una frase: tema claro/oscuro por `data-theme` en `<html>` con preferencia en localStorage (`txpl_theme`), command palette global (Ctrl+K) con registro `PALETTE_ACTIONS` verificado por `frontend-handlers.test.js`, y helper `emptyState()` en core.js para estados vacíos.

- [ ] **Step 2: Actualizar README.md**

En la lista de características de la UI, añadir: tema claro/oscuro, búsqueda global Ctrl+K y uso completo desde el móvil.

- [ ] **Step 3: Verificación final completa**

```bash
npm test
```
Expected: PASS todos. Revisar a mano en `npm run dev`: los dos temas en las páginas principales, palette, estados vacíos, móvil.

- [ ] **Step 4: Commit**

```bash
git add README.md CLAUDE.md
git commit -m "docs: UX pack (tema claro/oscuro, palette Ctrl+K, estados vacíos, responsive)"
```
